<import src='shopping-cart-template/shopping-cart-template.wxml'/>

<view class='container'>

  <template is="{{goodsList.length>0 ? 'shopping-cart-goods-list':'no-goods'}}" data='{{isEditting,goodsList,totalPrice,totalScore,haveSelected,allSelected}}'/>

</view>